# Core runtime dependencies (installed with `pip install -r requirements.txt`)
Flask>=3.0
requests>=2.31
pypdf>=5.0
ebooklib>=0.18
beautifulsoup4>=4.12
html2text>=2020.1.16
Pillow>=10.0

# Development tooling (lint, tests, coverage, schema validation) lives in the
# `dev` extra: `pip install -e ".[dev]"`
